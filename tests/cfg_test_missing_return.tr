// Test: Missing return on some paths
// Expected: Should warn about missing return paths

fn int test_missing_return(int x) {
    if (x > 0) {
        return x
    } else {
        // Missing return here for x <= 0 case
        int dummy = 0
    }
}

// Top-level call
test_missing_return(5)
