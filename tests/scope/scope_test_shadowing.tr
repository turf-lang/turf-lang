// Test 3: Shadowing (should warn but compile)
// Outer scope variable is shadowed by inner scope variable
int x = 5;
print(x);

if x > 0 {
  int x = 10;  // Warning: shadows outer x
  print(x);    // Should print 10
} else {
  print(0);
}

print(x);      // Should print 5 (outer x)
