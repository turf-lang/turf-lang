// Test: If without else, needs return after
// Expected: Should compile successfully

fn int test_if_no_else(int x) {
    if (x < 0) {
        return -x
    } else {
        // This return is necessary for the x >= 0 case
        return x
    }
}

// Top-level call
test_if_no_else(-10)
